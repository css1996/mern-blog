

Day - 1
========

1. Create project:

    npm create vite@latest my-project -- --template react
    cd my-project

2. Install Tailwind CSS:
  
  Install tailwindcss and its peer dependencies, then generate your tailwind.config.js and postcss.config.js files.

   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p

3.  Configure your template paths:

     Add the paths to all of your template files in your tailwind.config.js file.
 
  /** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

4. Add the Tailwind directives to your CSS:

    Add the @tailwind directives for each of Tailwind’s layers to your ./src/index.css file.

    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    5. Start your build process:

         Run your build process with npm run dev.


   Day - 2 
   ========      

   # Create pages and routes

      => install  npm i react-router-dom     
    
 -> create pages folder
         - About.jsx, Dashboard.jsx, SignIn.jsx, SignUp.jsx, Home.jsx, Projects.jsx
         
 -> In App.js
       import {BrowserRouter, Routes, Route} from 'react-router-dom';
     and import all pages like About, home, SignIn,.....etc

     inside return 

    <BrowserRouter>
       <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/about" element={<About />} />
          <Route path="/sign-in" element={<SignIn />} />
          <Route path="/sign-up" element={<SignUp />} />
          <Route path="/dashboard" element={<Dashboard />} />
          <Route path="/projects" element={<Projects />} />
       </Routes>
    </BrowserRouter>

    Day - 3 
    ========

    # Create Header components
       
       - create component folder under src folder
       - component
            - Header.jsx
       - add Header.jsx file inside App.jsx 
            - after BrowserRouter and before Routes     

      - create a header folder we can use "flowbite"

      => Flowbite React is an open-source UI component library built with React components, Tailwind CSS utility classes and based on the Flowbite design system    

      # install flowbite command "npm i flowbite-react"

      - Add the Flowbite React content path and plugin to tailwind.config.js:
            - const flowbite = require("flowbite-react/tailwind");
             content: [
                    flowbite.content(),
                   ],
                   plugins: [flowbite.plugin(),
                   ]

        # install npm i react-icons

 *** Search functionality ***
     ====================       

        => For Search functionalites using  'react-icons/ai' and flowbite-react

        import {Button, TextInput} from 'flowbite-react';
        import {AiOutlineSearch} from 'react-icons/ai';

         <form>
          <TextInput 
              type='text'
              placeholder='Search...'
              rightIcon={AiOutlineSearch}
              className='hidden lg:inline'
          />
       </form>
       <Button className='w-12 h-10 lg:hidden' color='gray' pill>
        <AiOutlineSearch />
       </Button>